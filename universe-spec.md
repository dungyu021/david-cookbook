# 功能規格:料理宇宙(Dish Universe)— 3D 關係圖

## 概念

一個類似 Obsidian graph view 的 3D 力導向圖(force-directed graph),呈現所有料理與其標籤、食材之間的關係網絡,讓瀏覽者探索料理風格的融合(如泰式×義式)。這是 David 的「料理宇宙」。

## 技術選型(已決策)

- 使用 **`3d-force-graph`** 函式庫(vasturiano/3d-force-graph,基於 Three.js)。
- 建立獨立頁面 `/universe/`(三語:`/universe/`、`/en/universe/`、`/it/universe/`)。
- **Three.js 相關程式碼只在此頁載入**(lazy load / client-only island),絕不影響首頁與其他頁面的載入效能。
- 圖的資料在 build 時從 content collections 產生一份 `graph.json`(每語言一份,節點名稱隨語言翻譯)。

## 資料模型

- **節點兩種:**
  - `dish`(料理):橘色、大;附封面縮圖資訊供點擊面板使用
  - `attr`(標籤或食材):紫色、小;標籤與食材視覺上同色,但資料中保留 `subtype: tag | ingredient`
- **連線:**每道料理連到它的所有標籤與所有食材(份量不進圖,只用食材名稱)。
- **排除清單:**維護一份 `graph-exclude` 清單(初始值:水、鹽、糖、油這類基礎食材),清單中的食材不產生節點。清單放在容易編輯的設定檔中,David 之後會自行增減。
- 同名食材跨料理必須合併為同一節點(以 zh 名稱為 canonical key,翻譯版仍用同一 key 對應)。

## 視覺與互動規格

1. **節點外觀:**dish = 橘色大球;attr = 紫色小球。attr 節點的不透明度依連線數(degree)遞增:連越多越明顯,只連 1 條的最透明。具體透明度區間先實作一版,由 David 看實機效果後調整。
2. **點擊 attr 節點(紫色):**該節點、其所有連線、與所有相連的 dish 節點高亮;其餘節點與連線變暗。再點一次空白處或同節點取消高亮。
3. **點擊 dish 節點(橘色):**同樣高亮其鄰居,並顯示一個小卡片(菜名 + 封面縮圖 + 「查看這道菜」連結,連到料理詳細頁)。手機上卡片從底部彈出。
4. **鏡頭:**點擊任一節點時鏡頭平滑飛近該節點。支援滑鼠拖曳旋轉/滾輪縮放,手機單指旋轉/雙指縮放。
5. **標籤文字:**預設不顯示所有節點名稱(會太亂);hover(桌面)或點擊(手機)時顯示該節點名稱。
6. **孤立群集**:無需特殊處理,讓力導向模擬自然呈現。
7. **配色:**背景深色(深藍黑,像宇宙),節點顏色需與網站現有視覺風格協調。首頁需有入口連結(導覽列或顯眼位置,文案類似「探索料理宇宙 🪐」)。
8. **降級處理:**若裝置不支援 WebGL,顯示友善訊息與返回連結即可,不需做 2D 備援版。

## 實作步驟(一次做一步,每步結束後部署,由 David 在真實手機上驗收後才進下一步)

- **Step 1 — 資料:**build 時產生 `graph.json`(含排除清單機制與跨料理食材合併)。先用 console 或簡單頁面驗證節點/連線數量正確。
- **Step 2 — 基本渲染:**`/universe/` 頁面 + 3d-force-graph 基本圖(顏色、大小、透明度規則),可旋轉縮放。
- **Step 3 — 互動:**點擊高亮鄰居、鏡頭飛行、dish 卡片與跳轉連結、名稱標籤顯示。
- **Step 4 — 打磨:**手機觸控微調、配色與網站風格整合、三語節點名稱、首頁入口、效能檢查(此頁不納入 Lighthouse 90+ 的硬性要求,但需在中階手機上流暢)。

## 驗收原則

視覺參數(球的大小、線的粗細顏色、透明度曲線、飄動阻尼、飛行速度)高度主觀,預期 David 會以白話描述感受(「太擠」「太亮」「轉太快」),來回多輪調整屬正常流程。每輪調整只改參數,不重寫架構。
